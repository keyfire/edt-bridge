# edt-bridge-mcp

[English](https://github.com/keyfire/edt-bridge/blob/main/python/README.md) · **Русский**

stdio MCP-обвязка для плагина [edt-bridge](https://github.com/keyfire/edt-bridge) к 1C:EDT.

Java-плагин отдаёт MCP как простой JSON-RPC по HTTP на `127.0.0.1:8770`. Значит, MCP-клиент,
настроенный на этот адрес, теряет сервер всякий раз, когда EDT не запущена. Вместо адреса клиент
разговаривает с этой обвязкой:

- **EDT открыта**, с интерфейсом или headless, и каждый запрос уходит живому мосту;
- **EDT закрыта**, и обвязка **сама поднимает headless EDT** – `1cedtcli` с поддерживающим
  конвейером, тот же рецепт, что в `scripts/run-headless.ps1`, – а как только модель готова,
  пробрасывает запросы;
- **jar плагина нет**, и обвязка **сама доставляет его** из последнего релиза GitHub, с проверкой
  контрольной суммы, в `dropins/` EDT перед запуском. Одного `pipx install edt-bridge-mcp`
  достаточно, чтобы мост заработал;
- сессия клиента не виснет на старте. Пока мост поднимается, `tools/list` возвращает пустой список,
  а как только он готов, приходит `notifications/tools/list_changed`.

![Как устроен мост](https://raw.githubusercontent.com/keyfire/edt-bridge/main/docs/architecture.ru.png)

## Установка

```bash
pipx install edt-bridge-mcp        # или из рабочей копии: pipx install ./python
```

## Регистрация в MCP-клиенте

```bash
claude mcp add edt-bridge -- edt-bridge-mcp --workspace "D:\\путь\\к\\workspace-EDT"
```

## Из командной строки

Режим по умолчанию говорит по JSON-RPC через stdin и stdout, его ведёт MCP-клиент. Чтобы достучаться
до того же моста руками или из скрипта, есть подкоманды: тот же перебор портов, тот же токен, тот же
автозапуск headless.

```bash
edt-bridge-mcp tools                    # что отдаёт запущенный мост
edt-bridge-mcp call edt_projects        # вызвать инструмент и напечатать результат
edt-bridge-mcp call edt_metadata_details --json '{"projectName": "SM", "fqn": "CommonModule.Foo"}'
edt-bridge-mcp call edt_create_extension --json-file args.json   # аргументы из файла UTF-8
edt-bridge-mcp status                   # поднят ли мост (сам ничего не запускает)
```

`--raw` печатает результат в JSON вместо текста, который вернул инструмент. Аргументы берутся из
`--json`, `--json-file` или `--stdin`, и для аргументов с не-ASCII текстом на Windows надёжнее файл.

Коды возврата: `0` – порядок, `1` – вызов вообще не состоялся (моста нет, ошибка использования или
транспорта), `2` – мост выполнил инструмент, и тот сообщил об ошибке. Так скрипт отличает "упало" от
"не запускалось".

## Самообновление

```bash
edt-bridge-mcp self-update             # обновить jar плагина (GitHub Releases) + обвязку (PyPI)
edt-bridge-mcp self-update --jar-only  # только jar
edt-bridge-mcp self-update --pip-only  # только обвязку
edt-bridge-mcp self-update --pip-only --from <repo>/python   # из рабочей копии
```

Запущенная EDT, с интерфейсом или headless, держит старый jar до перезапуска. Свой headless-экземпляр
обвязка перезапустит, когда будет поднимать его в следующий раз.

`--from` ставит обвязку из локальной рабочей копии, а не с PyPI. Так пробуют невыпущенную сборку, не
прибегая к `pipx install --force`: тот пересоздаёт venv и заменяет exe, который держит запущенный
клиент.

Код возврата отделяет мост от его дополнений. `0` – обновилось всё, что просили. `1` – не обновился
сам мост, jar или обвязка, либо не обновилось ничего. `2` – мост обновлён, а не встал только плагин
обвязки: плагин ставится из своего источника, и недоступность этого источника – не отказ моста
обновиться.

Обвязка обновляет себя распаковкой, а не установщиком. Она качает wheel-пакет с PyPI либо копирует
пакет из рабочей копии, указанной в `--from`, и заменяет пакет в `site-packages` одной стандартной
библиотекой. Ни pip, ни pipx, ни отдельного сборщика пакета, и это важно: pipx 1.15 создаёт venv
через uv, а такой venv pip не содержит вовсе.

## Плагины

Внешние пакеты, установленные в окружение обвязки, могут добавлять свои инструменты MCP. Это дом для
того, чему не место в публичном репозитории: справочные материалы под чужой лицензией, инструменты,
завязанные на внутренний сервис. Обвязка находит их через группу точек расширения
`edt_bridge.tools`, перечисляет их инструменты рядом с инструментами моста и выполняет сама, поэтому
они отвечают даже без запущенной EDT.

```bash
pipx inject edt-bridge-mcp <пакет>   # установить плагин
edt-bridge-mcp plugins               # что подключено и почему плагин не загрузился
```

`EDT_BRIDGE_NO_PLUGINS=1` выключает поиск плагинов. Контракт объявления для авторов – на
[странице установки](https://docs.keyfire.ru/edt-bridge/ru/install#плагины-обвязки).

Файлы `.exe` в `Scripts` не трогаются. Именно их держит запущенный клиент, Windows не даёт их
заменить, да и не нужно: заглушка при следующем запуске возьмёт тот код, который лежит в
site-packages. `pipx_metadata.json` поправляется, чтобы `pipx list` не показывал старую версию.
Editable-установку обновление не перезаписывает, а отклоняет.

## Настройка

Флаги CLI имеют приоритет над переменными окружения.

| Переменная | Флаг | Смысл |
|-----|------|---------|
| `EDT_BRIDGE_PORT` | `--port` | порт моста (по умолчанию 8770) |
| `EDT_BRIDGE_TOKEN` | – | токен инструментов записи; уходит как `Authorization: Bearer` и передаётся в JVM headless-экземпляра |
| `EDT_BRIDGE_WORKSPACE` | `--workspace` | путь к рабочей области EDT, обязателен для автозапуска headless |
| `EDT_BRIDGE_EDT_DIR` | `--edt-dir` | каталог установки EDT (`.../1cedt`); без него берётся самая свежая установка |
| `EDT_BRIDGE_START_TIMEOUT` | `--start-timeout` | сколько секунд ждать поднимающийся мост (по умолчанию 360) |
| `EDT_BRIDGE_AUTOSTART` | `--no-autostart` | `0` или флаг – только пересылать запросы и ничего не запускать |

Редко нужные (`EDT_BRIDGE_PORT_SCAN`, `EDT_BRIDGE_WINDOW_WAIT`, `EDT_BRIDGE_LANG`) и переменные
самого плагина собраны в разделе
[Переменные окружения](https://docs.keyfire.ru/edt-bridge/ru/install#переменные-окружения).

## Безопасность

- Если **EDT с интерфейсом запущена, а порт моста мёртв** – то есть плагина там нет, – обвязка
  отказывается поднимать headless: рабочую область держит запущенная EDT. Недостающий jar она при
  этом в `dropins/` доставит, и перезапуск этой EDT активирует мост.
- Если headless `1cedtcli` уже стартует, обвязка ждёт его, а не порождает второй.
