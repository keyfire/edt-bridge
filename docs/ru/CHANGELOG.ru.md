[English](../../CHANGELOG.md) · **Русский**

# История изменений

Все заметные изменения edt-bridge фиксируются здесь.

Формат основан на [Keep a Changelog](https://keepachangelog.com/ru/1.1.0/), проект следует
[семантическому версионированию](https://semver.org/lang/ru/). Jar плагина и обвязка
`edt-bridge-mcp` используют один номер версии.

## [Не выпущено]

### Добавлено
- `edt_infobase_config_state` – сравнивает основную конфигурацию базы с *конфигурацией базы данных*,
  той, которую исполняют сеансы, выгружая обе через `ibcmd`. База адресуется путём или координатами
  СУБД, поэтому не нужен ни доступ к кластеру, ни сеанс EDT.

  Результат намеренно несимметричен, и об этом сказано в описании инструмента: одинаковые файлы
  означают, что конфигурация ПРИМЕНЕНА, а различие НЕОДНОЗНАЧНО. Динамическое обновление оставляет
  контейнеры разными даже тогда, когда платформа сама сообщает, что обновление базы данных не
  требуется – воспроизведено на файловой базе без единого сеанса и при побайтно равных размерах
  файлов, то есть отличается служебная часть контейнера, а не содержание. Чтобы отличить "не
  применяли" от "применили динамически", нужен признак, которого нет: `--db` есть только у
  `config save|sign`, `config export info|status` требуют каталог XML-выгрузки, а не `.cf`, а
  `config generation-id` отражает ОСНОВНУЮ конфигурацию, и сравнивать его не с чем.
- `edt_infobase_dump` – выгрузка информационной базы в `.dt` через `ibcmd`: резервная копия, которую
  стоит снимать перед применением конфигурации к базе данных и которую мост снять не мог. По умолчанию
  dry-run, перезаписывать существующий файл отказывается.
- Инструменты поверх ibcmd принимают учётные данные информационной базы 1С (`infobaseUser` /
  `infobasePassword`) наряду с учёткой СУБД. Их легко перепутать, и разница не косметическая: база с
  настроенными пользователями отказывает без учётки 1С, а ibcmd после этого *запрашивает имя
  пользователя на stdin* – для неинтерактивного вызова это зависание, а не ошибка. Перенаправить stdin
  в пустое устройство недостаточно: ibcmd игнорирует EOF и печатает приглашение бесконечно (замерено:
  134 МБ за 60 секунд). Поэтому вывод читается порциями, а процесс снимается сразу, как только
  появляется приглашение или вывод начинает разрастаться – вызов теперь падает за две секунды с
  сообщением, какой учётки не хватает.
- Каждый вызов ibcmd получает собственный рабочий каталог. Без `--data` все они блокируют один и тот
  же каталог автономного сервера, и следующий вызов где угодно падает с "рабочий каталог заблокирован
  процессом".
- Там, где ibcmd не может сделать запрошенное, инструменты объясняют причину, а не сваливают вину на
  координаты: в режиме `extension` нет `--user` вообще, поэтому расширения базы с аутентификацией
  пользователей этим путём недоступны, о чём и сказано в ответе.
- `edt_update_infobase` рядом с `equality` возвращает `equalityNote`, где прямо сказано: сравнение
  касается ОСНОВНОЙ конфигурации и не означает, что конфигурация базы данных применена.
- `edt_extension_properties` принимает `infobase` – имя или uuid базы, зарегистрированной в EDT, –
  вместо расписывания адреса, а также учётные данные 1С. Это работает для ФАЙЛОВОЙ базы, путь которой
  EDT хранит. Для серверной не работает, и это не упущение: серверная база зарегистрирована в EDT
  координатой КЛАСТЕРА 1С (сервер и имя базы), а она не содержит координат СУБД, нужных ibcmd.
  Инструмент теперь так и говорит, вместо невнятного отказа, поэтому серверная база по-прежнему
  адресуется координатами СУБД.

### Изменено
- Набор тестов обвязки и воркфлоу `ci` (Linux и Windows, 3.10 и 3.12), который оба релизных воркфлоу
  вызывают первым шагом – красный набор останавливает релиз, а не выпускается мимо него. Его ядро –
  три регресса, которые действительно уехали в релиз: кадр `tools/list`, обрывавшийся на stdout в
  cp1251 (клиент регистрировал ноль инструментов), версия обвязки, разошедшаяся на два релиза, и
  `BrokenPipeError` при конвейере в `head`.
- Покрытие JUnit для EDT-независимой части плагина. Определение исходников объекта по FQN и решение,
  относится ли проблема валидации к запрошенному, переехали в `...edtbridge.core` – там нет типов EDT
  и Eclipse, поэтому их компилирует и проверяет обычный JDK: `scripts/test-java.sh` не кладёт в
  classpath ничего, кроме JUnit, так что зависимость от SDK ломает сборку. Заодно ушла дублирующая
  копия карты папок, которую распознаватель модулей и фильтр проблем держали порознь.

## [0.6.0] – 2026-07-21

### Добавлено
- `edt_add_route` – добавляет маршрут (шаблон URL и один HTTP-метод) в `HTTPService`. Мост закрывал
  реквизиты, формы и код модулей, но новый маршрут означал правку `.mdo` сервиса руками и генерацию
  `uuid` и для блока `urlTemplates`, и для вложенного `methods` – ровно та ручная правка, ради отказа
  от которой мост и существует. По умолчанию dry-run: находит сервис, проверяет, что имя шаблона
  свободно, а HTTP-метод разрешается в литерал перечисления `httpMethod`, и возвращает план.
  `apply=true` создаёт оба объекта через модель и сериализует `.mdo`; `createHandler` вставляет
  заготовку `Функция <handler>(Запрос)` в модуль сервиса.

### Изменено
- `edt_project_errors` теперь отвечает на вопрос "что не так с модулем, который я только что правил".
  Инструмент принимал имя проекта и возвращал всё: на большой конфигурации это 13 850 проблем, ~5 МБ
  и 152 000 строк – больше любого лимита результата, поэтому каждый вызов сваливался в файл, который
  потом фильтровали внешним скриптом. Появились необязательные `fqn` / `modulePath` (сузить до одного
  объекта или модуля), `severity` (ERROR/WARNING/INFO) и `countOnly` (только счётчики, без списка –
  то, что нужно для сверки "до и после"). В результате также есть `total`, `totalBeforeFilter`,
  `bySeverity` и `bySource`, а список ограничен `limit` (по умолчанию 1000, признак `truncated`),
  поэтому и вызов без фильтров остаётся в разумных пределах.
- У `edt_module_text` появился `includeMethods` (по умолчанию true). Каждый вызов возвращал весь
  каталог процедур и функций модуля с сигнатурами, даже когда запрашивался один `method` – 147
  методов у одного рабочего модуля, и так при каждом чтении. `false` возвращает только запрошенный
  текст.
- `edt_module_text`, `edt_add_method` и `edt_delete_method` разрешают FQN вида `HTTPService.X` и
  `WebService.X`. `edt_delete_method` отказывал с сообщением "unsupported object type: HTTPService
  (pass modulePath directly)", заставляя вызывающего собирать путь `src/HTTPServices/<имя>/Module.bsl`
  вручную; оба вида сервисов просто отсутствовали в карте папок у распознавателя модулей, поэтому
  один пробел задевал все три инструмента.

## [0.5.0] – 2026-07-19

### Исправлено
- `self-update` не мог обновить обвязку на актуальном pipx. Он звал `pip install --upgrade` в своём
  окружении – а pipx 1.15 создаёт venv через uv, и в таком venv pip нет вообще, поэтому шаг падал
  с "No module named pip", и обвязка никогда себя не обновляла.

  Установщик больше не используется. Колесо качается с PyPI и распаковывается поверх пакета в
  `site-packages` одной стандартной библиотекой; файлы `.exe` в `Scripts` не трогаются – именно их
  держит запущенный клиент, а стаб подхватит новый код сам; `pipx_metadata.json` поправляется,
  чтобы `pipx list` перестал показывать старую версию. Editable-установка отклоняется, а не
  перезаписывается. Соседние инструменты так делали и раньше – трудным путём шла только обвязка.
- Обвязка сообщала устаревшую версию: `--version` и `serverInfo` в MCP говорили 0.3.1 два релиза
  подряд, потому что литерал в `__init__.py` не использовался в `pyproject.toml`. Теперь версия
  пакета выводится из этого самого атрибута, и разойтись они больше не могут.
- `edt_create_extension` создавал проект, который не принимала ни одна информационная база: загрузка
  падала с ошибкой "Загрузка не должна менять принадлежность основного объекта конфигурации", причём
  и на пустом расширении – значит, дело было не в содержимом. Корневую `Configuration` строила
  md-фабрика, а она даёт обычную **полную** конфигурацию: нет `objectBelonging=Adopted`, нет блока
  управления `ConfigurationExtension`, нет `keepMappingToExtendedConfigurationObjectsByIDs`, нет
  заимствованного языка, не унаследованы вариант встроенного языка и режимы совместимости – зато
  есть свойства полной конфигурации, которым в расширении не место. Теперь корень расширения – это
  **заимствованная базовая конфигурация**, ровно тот шаг, который делает штатный мастер нового
  расширения EDT (`ExtensionWizard.adopt`), и всё перечисленное пишет сам движок.

  Вместе с этим закрылись ещё две проблемы – обе оказались следствиями, а не причинами:
  - у объектов, заимствованных `edt_adopt_object`, появился `extendedConfigurationObject` –
    uuid-связь с базовым объектом – вместо связывания по имени. EDT пишет эту связь только когда у
    `Configuration` расширения стоит `keepMappingToExtendedConfigurationObjectsByIDs`, а его никто
    не выставлял.
  - язык по умолчанию базовой конфигурации теперь приходит сам, заимствованный как положено, с
    `languageCode` и uuid-связью. Языки не top-объекты, поэтому заимствовать их поштучно было нечем;
    их приносит именно заимствование конфигурации.
- `edt_create_extension` вычитывает созданную `Configuration` обратно и сообщает, что реально легло
  (`objectBelonging`, `extensionBlock`, `keepMappingByIds`, `adoptedLanguage`, `languageLinked`).
  Раньше поломка была не видна, пока сборку не отвергала информационная база, – это поздно.

### Добавлено
- У `self-update` появился `--from <checkout>`: обвязка ставится из локальной рабочей копии, а не с
  PyPI, – чтобы попробовать невыпущенную сборку, не прибегая к `pipx install --force`, который
  пересоздаёт venv и заменяет exe, удерживаемый запущенным клиентом.
- `edt-bridge-mcp` управляется из командной строки: `call <инструмент>` вызывает один инструмент и
  печатает результат, `tools` перечисляет то, что отдаёт мост, `status` показывает, поднят ли он.
  Раньше обвязка говорила только по JSON-RPC через stdin/stdout, поэтому всё за пределами
  MCP-клиента означало писать свой клиент к порту 8770 – в том числе проверку только что собранного
  инструмента, ведь новый инструмент не виден клиенту до его перезапуска. Аргументы берутся из
  `--json`, `--json-file` или `--stdin`; `--raw` печатает JSON-результат. Коды возврата отделяют
  "вызов не состоялся" (1) от "инструмент отработал и сообщил об ошибке" (2).
- `edt_extension_properties` – чтение и установка свойств, с которыми расширение зарегистрировано
  **в информационной базе** (безопасный режим, защита от опасных действий, активность, область
  действия), через `ibcmd extension info|list|update`. Это свойства регистрации в базе, и их не
  задаёт ничто из того, чем расширение туда попадает: только что зарегистрированное расширение
  получает безопасный режим и защиту от опасных действий **включёнными** – проверено, это
  умолчания самого ibcmd, а обновление из EDT их не трогает. Расширение, меняющее методы базовой
  конфигурации, не работает ни под тем, ни под другим, поэтому оба флага нужно снимать, – а раньше
  об этом ничто не предупреждало, пока расширение не начинало вести себя неправильно в базе.
- `edt_build_extension` и `edt_update_infobase` сообщают `changesMethods` и, когда он истинен,
  называют два флага, которые нужно снять. Проверка читает модули проекта на аннотации перехвата
  (`&Вместо` / `&Перед` / `&После` / `&ИзменениеИКонтроль` и их английские написания).
- У `edt_create_extension` появился необязательный `synonym` – представление расширения, которое
  пишется для языка, заимствованного из базовой конфигурации, как и одноимённое поле мастера.

## [0.4.1] – 2026-07-19

### Добавлено
- `edt_search_modules` – полнотекстовый поиск по модулям BSL проекта (подстрока или регулярное
  выражение, опциональный фильтр пути). Чтение идёт через `ITextFileBufferManager` Eclipse, поэтому
  модуль, открытый в редакторе, ищется в текущем виде, вместе с несохранёнными правками, и у каждого
  попадания указано, откуда оно взято. Это был единственный шаг разведки, который всегда падал на
  дисковые инструменты: `edt_find_references` отвечает "кто вызывает этот метод", а этот – "где
  встречается такой текст".
- `edt_adopt_object` – заимствование объекта базовой конфигурации в проект расширения штатным
  `IModelObjectAdopter` EDT. Без него `edt_create_extension` останавливался на полпути: проект есть,
  а расширять нечего, потому что перехват метода требует сперва заимствовать владеющий объект. Движок
  сам пишет `objectBelonging`, блок пофайловых флагов управления свойствами и запись в
  `Configuration.mdo` расширения – всё это легко испортить, делая руками.
- `edt_metadata_details` возвращает флаги компиляции общего модуля (`server`, `serverCall`,
  `clientManagedApplication`, `clientOrdinaryApplication`, `externalConnection`, `privileged`,
  `global`, `returnValuesReuse`) и состав плана обмена – объекты, реплицируемые между узлами, с
  режимом авторегистрации. И то и другое раньше заставляло грепать `.mdo` на диске.

## [0.4.0] – 2026-07-19

### Добавлено
- `edt_clean_project` – сбросить результаты сборки проекта, чтобы валидация отработала заново;
  программный аналог диалога "Очистить" в EDT. Проверки EDT висят на сборке, поэтому именно это
  заставляет их переиграть: иначе замечание переживает исправление кода, а устаревший маркер хуже,
  чем никакого. Возвращает число замечаний до и после, дождавшись, пока оно перестанет меняться.
- `edt_add_form` – создание управляемой формы у существующего объекта метаданных. Инструмент
  использует штатный `IFormGenerator` EDT – движок мастера "Новая форма" – и повторяет
  последовательность `FormNewWizardRelatedModelsFactory`: создать запись `BasicForm`, связать её со
  свойством `forms` владельца, сгенерировать содержимое формы, связать пару через `setMdForm`,
  подключить форму как топ-объект BM под FQN от `ITopObjectFqnGenerator`, затем записать модуль из
  `generateModuleContent`. Вид формы принимается и как имя `FormType`, и как русский синоним
  (`ФормаЭлемента`, `ФормаСписка`, `ФормаЗаписи`, ...); если вид не задан, берётся обычный для
  владельца. По умолчанию dry-run, под токеном, аддитивно – force не нужен.
- Элементы формы: `edt_add_form_item`, `edt_modify_form_item`, `edt_remove_form_item`. Элементы
  создаются штатным `IFormItemManagementService` – тем самым, который вызывает редактор форм, –
  поэтому имена, идентификаторы, фактический вид поля и автозаполнение колонок таблицы определяет
  EDT, а не ручная сборка. Виды: поле, таблица, кнопка, группа (обычная группа, страница, страницы,
  командная панель, группа кнопок, группа колонок, подменю) и декорация. Dry-run проверяет путь к
  данным по реквизитам формы, а команду – по её командам, поэтому неверная связка роняет план.
  Удаление забирает всё вложенное и требует `force`.
- Члены формы, полный набор для обоих видов: `edt_add_form_attribute`, `edt_modify_form_attribute`,
  `edt_remove_form_attribute`, `edt_add_form_command`, `edt_modify_form_command`,
  `edt_remove_form_command`. Идентификаторы выдаёт штатный `FormIdentifierService`. Типы реквизитов
  используют грамматику `edt_add_attribute` и дополнительно принимают платформенные типы, допустимые
  у формы (`ТаблицаЗначений`, `СписокЗначений`, ...), которые резолвятся через провайдер
  платформенных типов; тип теперь строится ещё в dry-run, поэтому негодная строка типа роняет план,
  а не применение. `edt_add_form_command` умеет записать заготовку процедуры-обработчика в модуль
  формы, создав его, если модуля не было. Удаления перечисляют, что ещё связано с членом, – элементы
  для реквизита, кнопки для команды – и требуют `force`. Три инструмента реквизита принимают также
  `columnOf`: он адресует КОЛОНКУ реквизита типа ТаблицаЗначений вместо реквизита формы – колонка
  это тот же `AbstractFormAttribute`, поэтому отдельных инструментов ей не нужно. Когда колонки
  заданы, поля колонок таблицы ставятся через `edt_add_form_item` (`parent` – таблица, `dataPath` –
  `реквизит.колонка`).
- `edt_delete_project` – удаление проекта из workspace, замыкающее цикл, который открывают
  инструменты создания проектов. Удаление идёт через workspace Eclipse, поэтому дерево ресурсов
  обновляется; если снести каталог проекта руками, остаётся фантом – проект возвращается из снимка
  дерева при следующем старте и держит имя занятым. `deleteContent` выбирает между снятием
  регистрации и стиранием файлов. По умолчанию dry-run, для применения нужен `force`.
- `edt_create_external_object` принимает `scriptVariant` (Russian / English) и применяет его через
  `IExternalObjectProjectManager.setScriptVariant` / `setLanguages`. EDT создаёт standalone-проект
  с английским вариантом, из-за чего генерируемые члены получались `Object`, а не `Объект`; dry-run
  теперь предупреждает, если вариант не задан у standalone-проекта.

### Исправлено
- `edt_dump_external_object` снова собирает `.epf`/`.erf` там, где EDT не может. EDT резолвит
  НОВЕЙШУЮ сборку линии версии, поэтому линия, во главе которой стоят тонкие клиенты, роняла штатный
  dumper с `MatchingRuntimeNotFound` даже при наличии полных установок этой линии. Добавлен дисковый
  маршрут - экспорт в XML конфигуратора внутри процесса, затем сборку выполняет полная платформа,
  найденная на диске, - по образцу дискового фоллбека создания информационной базы. Заодно перестал
  врать dry-run: штатная `validateDumpGeneration` возвращает OK независимо от наличия толстого
  клиента, поэтому платформа проверяется напрямую, а план называет маршрут. Новый `logPath` пишет лог
  сборки рядом с артефактом (по умолчанию `<targetPath>.log`) - именно там платформа объясняет
  причину отказа.
- Модули внешних объектов резолвятся по FQN. В карте "тип метаданных - каталог" не было внешних
  объектов, поэтому `ExternalDataProcessor.X.Form.Y` отвергался с "unsupported owner type for a form"
  в `edt_add_method`, `edt_delete_method` и `edt_module_text` (обходилось передачей `modulePath`).
  Теперь `ExternalDataProcessor` и `ExternalReport` в карте есть. Расширений конфигурации это никогда
  не касалось - раскладка их исходников совпадает с конфигурацией.

### Изменено
- MCP-сервер сообщает собственную версию плагина, взятую из манифеста бандла, вместо зашитой строки:
  релиз поднимает её в одном месте, и страница состояния с рукопожатием не могут разъехаться.

## [0.3.1] – 2026-07-19

### Исправлено
- Обвязка stdio фиксирует стандартные потоки в UTF-8. На Windows они открывались в кодировке ANSI,
  и символ "→" в описании `edt_modify_attribute` ронял `tools/list` ошибкой кодирования `charmap` –
  клиент в результате не получал ни одного инструмента. Кириллица в аргументах на входе была
  подвержена тому же дефекту.
- В таблицах инструментов README не хватало четырёх инструментов, добавленных в 0.3.0
  (`edt_platform_installations`, `edt_register_platform`, `edt_create_infobase`,
  `edt_build_extension`) – теперь они перечислены.

## [0.3.0] – 2026-07-19

### Добавлено
- `edt_platform_installations` – показывает установки платформы 1С:Предприятие, которые EDT
  резолвит (для сборки бинарников и создания баз), включая полные установки, найденные на диске.
- `edt_create_infobase` – создание пустой файловой базы через EDT, с фоллбеком на поиск подходящей
  полной установки платформы на диске, если у EDT её нет.
- `edt_register_platform` – регистрация установки платформы в EDT.
- `edt_build_extension` – сборка расширения конфигурации (`.cfe`) через `ibcmd`: экспорт проекта
  средствами EDT и упаковка во временной базе, которая затем удаляется.
- Страница настроек EDT-Bridge (токен, порт MCP-сервера и переключатель evaluate), чтобы GUI-EDT,
  запущенная из обычного ярлыка, могла аутентифицировать инструменты записи.
- Раздел "Ошибки и обратная связь" в README; workflow trusted publishing для PyPI.

### Исправлено
- `edt_infobases` теперь разворачивает группы баз – групповые/серверные базы больше не теряются.

### Изменено
- Внутреннее: единый модельный шлюз разбит на сфокусированные гейтвеи по областям (проекты, чтение
  и запись метаданных, формы, платформа, отладка, BSL, документация) плюс общие хелперы. Чистый
  рефакторинг – поведение не меняется.

## [0.2.1] – 2026-07-18

### Изменено
- Сплошная типографика по стилю проекта (среднее тире вместо длинного, три точки вместо символа
  многоточия), включая описания инструментов и Python-обвязку.

## [0.2.0] – 2026-07-18

### Добавлено
- `edt_platform_help` – Синтакс-помощник платформы 1С:Предприятие из поставки EDT (реальная справка
  API, с поиском, рус + англ).
- Многопоточный MCP-сервер (ограниченный пул воркеров).
- Фоллбек порта: если 8770 занят, берётся следующий свободный, и обвязка его находит.

### Исправлено
- `apply` создания проектов теперь работает надёжно (строится и привязывается свежий детаченный
  Configuration / внешняя обработка, а не роняется жизненный цикл headless-EDT).

## [0.1.1] – 2026-07-18

### Добавлено
- Инструменты записи цикла создать / разработать / собрать / доставить (по умолчанию dry-run).

### Исправлено
- `apply` создания проектов приведён к жизненному циклу headless-EDT.

## [0.1.0] – 2026-07-18

### Добавлено
- Полный цикл создать / разработать / собрать / доставить, открытый по MCP.
- Stdio-обвязка `edt-bridge-mcp` (pipx): пробрасывает запросы в запущенную EDT, сама поднимает
  headless-EDT, когда ни одна не открыта, и доставляет jar плагина в `dropins/` EDT.

## [0.0.1] – 2026-07-11

### Добавлено
- Первый выпуск: инструменты чтения по MCP (проекты, ошибки валидации, свойства и перечень
  метаданных, ссылки, валидация запросов, формы), встроенный дашборд и localhost MCP-сервер.

[Не выпущено]: https://github.com/keyfire/edt-bridge/compare/v0.3.0...HEAD
[0.3.0]: https://github.com/keyfire/edt-bridge/compare/v0.2.1...v0.3.0
[0.2.1]: https://github.com/keyfire/edt-bridge/compare/v0.2.0...v0.2.1
[0.2.0]: https://github.com/keyfire/edt-bridge/compare/v0.1.1...v0.2.0
[0.1.1]: https://github.com/keyfire/edt-bridge/compare/v0.1.0...v0.1.1
[0.1.0]: https://github.com/keyfire/edt-bridge/compare/v0.0.1...v0.1.0
[0.0.1]: https://github.com/keyfire/edt-bridge/releases/tag/v0.0.1
